// Search form: origin, destination, date picker.
// TODO (step 5): controlled inputs, client-side validation with clear error
// messages, and submit handling that triggers the search.

export default function SearchForm() {
  return (
    <section>
      {/* TODO: origin input, destination input, date picker, submit button */}
      <p>Search form goes here.</p>
    </section>
  );
}
