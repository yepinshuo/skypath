// Results display: itinerary segments, layovers, total duration and price.
// TODO (step 5): render each itinerary's segments/layovers/totals, plus
// loading, empty, and error states.

export default function Results() {
  return (
    <section>
      {/* TODO: render itineraries (segments, layovers, totals) + states */}
      <p>Results go here.</p>
    </section>
  );
}
